x-bwrap-security: &bwrap-security
  cap_add:
    - SYS_ADMIN
  security_opt:
    - apparmor=unconfined
    - seccomp=unconfined

services:
  pawbot-gateway:
    <<: *bwrap-security

  pawbot-api:
    <<: *bwrap-security

  pawbot-cli:
    <<: *bwrap-security
